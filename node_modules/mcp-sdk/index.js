const { ModelContext, Model } = require('./mcp');
const { underDevelopment } = require('./development');

underDevelopment();

const context = new ModelContext('JTBD');
const model = new Model('Claude-3.5-sonnet', context);

model.processModelData().then((data) => {
  console.log('Processed Data:', data);
});
